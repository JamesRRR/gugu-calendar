<view class="create-container">
  <form bindsubmit="submitForm">
    <!-- 基本信息卡片 -->
    <view class="form-card">
      <view class="card-title">
        <text class="icon">🌸</text>
        <text>基本信息</text>
      </view>
      
      <view class="form-item">
        <text class="label required">活动名称</text>
        <input 
          class="input" 
          name="title" 
          placeholder="给活动起个可爱的名字吧~" 
          value="{{formData.title}}" 
          bindinput="onTitleInput" 
        />
      </view>

      <view class="form-item">
        <text class="label required">开始时间</text>
        <view class="time-container">
          <picker
            mode="multiSelector"
            range="{{[dateOptions, timeOptions]}}"
            value="{{startPickerValue}}"
            bindchange="onStartDateTimeChange"
          >
            <view class="picker {{(formData.startDate && formData.startTime) ? '' : 'empty'}}">
              <text class="icon">🕒</text>
              <text>{{(formData.startDate && formData.startTime) ? (formData.startDate + ' ' + formData.startTime) : '选择开始日期时间'}}</text>
            </view>
          </picker>
        </view>
      </view>

      <view class="form-item">
        <text class="label required">结束时间</text>
        <view class="time-container">
          <picker
            mode="multiSelector"
            range="{{[dateOptions, timeOptions]}}"
            value="{{endPickerValue}}"
            bindchange="onEndDateTimeChange"
          >
            <view class="picker {{(formData.endDate && formData.endTime) ? '' : 'empty'}}">
              <text class="icon">⏰</text>
              <text>{{(formData.endDate && formData.endTime) ? (formData.endDate + ' ' + formData.endTime) : '选择结束日期时间'}}</text>
            </view>
          </picker>
        </view>
      </view>
    </view>

    <!-- 活动设置卡片 -->
    <view class="form-card">
      <view class="card-title">
        <text class="icon">✨</text>
        <text>活动设置</text>
      </view>

      <view class="form-item">
        <text class="label">活动地点（可选）</text>
        <view class="location-picker" bindtap="chooseLocation">
          <text class="icon">📍</text>
          <text class="location-text {{!formData.location.name && 'empty'}}">
            {{formData.location.name || '点击选择活动地点~'}}
          </text>
        </view>
      </view>

      <view class="form-item">
        <text class="label">人数限制</text>
        <input 
          class="input" 
          type="number" 
          name="maxParticipants" 
          placeholder="不填则不限制人数哦~" 
          value="{{formData.maxParticipants}}" 
        />
      </view>

      <view class="form-item">
        <text class="label">咕咕点数要求</text>
        <view class="slider-container">
          <view class="slider-value">需要 {{formData.regretPointsRequired}} 点咕咕点数</view>
          <slider 
            min="1" 
            max="10" 
            value="{{formData.regretPointsRequired}}" 
            activeColor="#ff8c98"
            backgroundColor="#ffe4e6"
            block-color="#ff8c98"
            show-value="{{false}}"
            bindchange="onRegretPointsChange" 
          />
        </view>
        <text class="tips">咕咕时将扣除 {{formData.regretPointsRequired}} 点咕咕点数~</text>
      </view>
    </view>

    <!-- 活动详情卡片 -->
    <view class="form-card">
      <view class="card-title">
        <text class="icon">💝</text>
        <text>活动详情</text>
      </view>
      
      <view class="form-item">
        <text class="label">活动简介</text>
        <textarea 
          class="textarea" 
          name="description" 
          placeholder="写点什么来介绍这个活动吧~" 
          maxlength="500" 
          value="{{formData.description}}" 
          bindinput="onDescriptionInput"
        />
        <text class="word-count">{{descriptionLength}}/500</text>
      </view>
    </view>

    <button class="submit-btn" form-type="submit">
      <text class="icon">🎀</text>
      <text>创建活动</text>
    </button>
  </form>

  <!-- 子页面不显示系统 tabBar：这里提供返回主界面入口 -->
  <view class="subpage-footer-nav">
    <view class="nav-item" bindtap="goToTab" data-index="0">
      <icon type="success" size="22" />
      <text>已注册</text>
    </view>
    <view class="nav-item active" bindtap="goToTab" data-index="1">
      <icon type="add" size="22" />
      <text>创建</text>
    </view>
    <view class="nav-item" bindtap="goToTab" data-index="2">
      <icon type="personal" size="22" />
      <text>我的</text>
    </view>
  </view>
</view> 